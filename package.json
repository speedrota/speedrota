{
  "name": "speedrota-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd speedrota-api && npm start",
    "build": "cd speedrota-api && npm install && npx prisma generate",
    "postinstall": "cd speedrota-api && npm install"
  },
  "dependencies": {
    "pino-pretty": "^13.1.3"
  }
}
