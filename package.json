{
  "name": "speedrota-monorepo",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node --import tsx index.ts",
    "build": "cd speedrota-api && npm install && npx prisma generate && npx prisma db push",
    "postinstall": "cd speedrota-api && npm install"
  },
  "dependencies": {
    "pino-pretty": "^13.1.3",
    "tsx": "^4.7.0"
  }
}
